import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#262626',
    },
    secondary: {
      main: '#1a8cff',
    },
    background: {
      default: '#FAFAFA',
    }
  },
});

export default theme;